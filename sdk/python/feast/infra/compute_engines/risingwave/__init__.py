"""RisingWave compute engine (contrib).

RisingWave is the real-time computation + online-serving plane; Feast keeps the
registry/governance and the point-in-time training joins. See ``README.md`` for the
architecture, the validated vs. not-yet-validated surfaces, and the ``feature_store.yaml``
wiring.
"""
